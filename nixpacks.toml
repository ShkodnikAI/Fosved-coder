[tools]
build = ["python"]

[phases.setup]
nixPkgs = ["...", "python311", "python311Packages.pip", "git"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn run:app --host 0.0.0.0 --port $PORT"
