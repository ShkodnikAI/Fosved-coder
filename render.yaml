services:
  - type: web
    name: fosved-coder
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn run:app --host 0.0.0.0 --port $PORT --ws-ping-interval 15 --ws-ping-timeout 10
