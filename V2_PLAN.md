> **Статус документа: ИСТОРИЧЕСКИЙ (на 2026-05-25).**
> Большая часть пунктов выполнена. Документ сохранён для понимания того, как
> проект задумывался. Актуальное состояние смотри в README.md и в коде.
>
> Расхождения с реальностью (на момент закрытия наряда № 4):
> - `core/chat.py` — упомянут как «удалить (deprecated)»; уже удалён.
> - `core/router.py` — упомянут; реальное имя `core/intelligent_router.py`.
> - Единая точка маршрутизации (1.3) — реализована в `_build_models_to_try` (наряд № 2).
> - Тихое зондирование (4.1) — реализовано (`probe_selected_models` в `agent.py`).
> - Динамическая анкета (4.2) — реализована (эндпоинты `/questionnaire`, `/drafts`).
> - Авто-выбор скиллов (4.3) — реализован (`_load_skill_context` в `agent.py`).

---

# FOSVED CODER V2.0 — План реализации

## Архитектурные принципы

### 1. Паттерн «Прокладка» (Middleware)
Модель НЕ знает о существовании инструментов. Ассистент молча:
- Подставляет содержимое файлов в контекст (prompt_injector)
- Извлекает команды записи/чтения из ответа (response_parser)
- Выполняет их, пользователь видит только итог

### 2. Двойной режим
- **Tool Calling** — для моделей с нативной поддержкой (Claude, GPT, Gemini)
- **Prompt Injection** — универсальный fallback для всех остальных (Qwen, Llama, Ollama)

### 3. Разделение экранов
- **Главный экран** — творческое обсуждение, скиллы, промпт-драфты, анкета
- **Экран проекта** — код, инструменты, git, файловое дерево

---

## Фазы реализации

### Фаза 1: Backend Core (python)
**1.1. core/response_parser.py** — полная переработка
- Убрать дублирование с agent.py
- Парсинг: `<file path="...">`, ```lang:path, ```diff, `<command>`, `<git>`
- Clean text для отображения пользователю
- ActionExecutor с полной обработкой всех типов действий

**1.2. core/prompt_injector.py** — усиление
- Автоматическая инъекция списка скиллов в контекст
- Умное усечение контекста с приоритетом ключевых файлов
- Динамическая анкета (вопросы от AI для сбора требований)

**1.3. core/agent.py** — переработка
- Единая точка обработки сообщений (убрать дублирование с router.py)
- Тихое зондирование моделей при старте
- Улучшенное маршрутизирование (по приоритету/сложности)
- Поддержка extended thinking (Claude Opus/Sonnet 4)

**1.4. core/memory.py** — очистка
- Удалить ChatThread (deprecated)
- Добавить таблицу Questionnaire (для динамической анкеты)
- Обновить таблицу Project (добавить поля для проектных карт)

**1.5. Очистка мёртвого кода**
- Удалить core/chat.py (deprecated)
- Убрать дублирующую маршрутизацию из router.py или agent.py

### Фаза 2: API (endpoints)
**2.1. api/endpoints.py** — реструктуризация
- Новые эндпоинты для экрана проектов
- Эндпоинты для динамической анкеты
- Эндпоинты для тихого зондирования
- Разбить на подмодули (опционально, в рамках одного файла)

**2.2. run.py** — обновление
- Новая логика WebSocket для двух экранов
- Обработка новых событий (анкета, зондирование)

### Фаза 3: UI (Frontend)
**3.1. ui/templates/index.html** — полная перестройка
- Навигация: главный экран / экран проекта
- Главный экран: чат + скиллы + промпт-драфты + анкета
- Экран проекта: файловое дерево + редактор + git + терминал
- Авто-переход между экранами

**3.2. ui/static/style.css** — обновление
- CSS-переменные для тем
- Компоненты для новых экранов
- Адаптивность

### Фаза 4: Новые функции
**4.1. Тихое зондирование**
- При старте → минимальный запрос к каждой модели
- Ответившая модель автоматически появляется в панели

**4.2. Динамическая анкета**
- AI задаёт адаптивные вопросы по проекту
- Ответы заполняют проектную карту
- Автоматическая генерация промпта из анкеты

**4.3. Авто-выбор скиллов**
- Список скиллов инжектится в системный промпт
- Модель сама выбирает нужные скиллы

---

## Фаза 5: Семантический анализ кода (CodeGraph)

> **Статус**: исследование завершено, интеграция отложена до Слоя 3+.
> Оригинал: https://github.com/colbymchenry/codegraph

### Что такое CodeGraph

Локальная система семантического анализа кода, строящая граф знаний из кодовой базы
с помощью tree-sitter AST. Хранит всё в SQLite (`<project>/.codegraph/codegraph.db`).
100% локально, никаких API-ключей.

**Метрики**: −35% стоимости, −57% токенов, −46% время, −71% tool calls (бенчмарк на 7 OSS-репо).

### Ключевые возможности

- **19+ языков**: Python, TypeScript, JavaScript, Go, Rust, Java, C#, PHP, Ruby, C/C++, Swift, Kotlin, Scala, Dart, Svelte, Vue, Lua и др.
- **Граф вызовов**: callers, callees, impact analysis, trace между символами
- **FTS5 полнотекстовый поиск** по именам, сигнатурам, докстрингам
- **Framework-aware**: распознаёт роуты FastAPI, Django, Express, NestJS, Laravel, Rails и ещё 14 фреймворков
- **MCP-сервер** (10 инструментов через stdio)
- **CLI** + **TypeScript API**
- **14+ framework-specific resolvers** для динамического диспатчинга

### Почему отложено

1. **Разные языки стеков**: fosved-coder = Python (FastAPI), CodeGraph = TypeScript/Node.js.
   Интеграция только через subprocess (CLI) или MCP stdio.
2. **Дополнительный Node.js runtime** в деплое — усложняет Docker/развертывание.
3. **Текущий `context_manager.py` не является узким местом** — он примитивен, но рабочий.
   Основная ценность fosved-coder — AI-агент с tool calling, глубина контекста вторична.
4. **WASM tree-sitter** требует Node 20–24 (заблокирован на 25 из-за бага V8).

### Предлагаемый вариант интеграции (когда придёт время)

**CodeGraph как дополнительный tool для AI-агента** (не замена `context_manager.py`):

```
codegraph context <task>       — построить семантический контекст для задачи
codegraph callers <symbol>     — кто вызывает символ
codegraph callees <symbol>     — что вызывает символ
codegraph impact <symbol>      — радиус влияния изменения
codegraph affected <files...>  — какие тесты затронуты
codegraph trace <A> <B>        — как A достигает B через вызовы
```

Реализация: subprocess-вызовы `codegraph` CLI из `core/executor.py`,
результат передаётся агенту как контекст. Альтернатива — MCP-клиент на Python.

### Предусловия для интеграции

- [ ] Фаза 1–3 V2 завершены
- [ ] `agent.py` разбит на модули (Слой 3) — иначе файл станет ещё больше
- [ ] Dockerfile поддерживает multi-runtime (Python + Node.js)
- [ ] Проведён бенчмарк CodeGraph на реальных проектах fosved-coder

---

## Порядок реализации (критический путь)

1. `core/response_parser.py` → фаза 1.1
2. `core/prompt_injector.py` → фаза 1.2
3. `core/agent.py` → фаза 1.3
4. `core/memory.py` → фаза 1.4
5. `api/endpoints.py` → фаза 2.1
6. `run.py` → фаза 2.2
7. `ui/templates/index.html` → фаза 3.1
8. `ui/static/style.css` → фаза 3.2
9. Тихое зондирование → фаза 4.1
10. Динамическая анкета → фаза 4.2
11. Семантический анализ кода → фаза 5
12. Умная система памяти → фаза 6

---

## Фаза 6: Умная система памяти (Smart Memory)

> **Статус**: исследование завершено, интеграция отложена до Слоя 3+.
> Оригинал: https://github.com/rohitg00/agentmemory

### Что изучено: agentmemory

Движок персистентной памяти для AI-кодинг агентов (18.7K ★, Apache-2.0).
Захватывает действия агента через 12 хуков, сжимает в векторизуемые факты,
инжектит релевантный контекст при старте сессии.

**Метрики**: 95.2% R@5 на LongMemEval-S (ICLR 2025), ~170K токенов/год
против ~19.5M при paste-full-context (−99% токенов).

### Ключевые идеи, применимые к fosved-coder

**1. 4-уровневая консолидация памяти (аналог человеческой)**

| Уровень | Что хранится | Аналог |
|---------|-------------|--------|
| Working | Сырые tool-outputs текущей сессии | Краткосрочная память |
| Episodic | Сжатые саммари сессий | «Что произошло» |
| Semantic | Извлечённые факты и паттерны | «Что я знаю» |
| Procedural | Рабочие процессы и решения | «Как я это делаю» |

**2. Гибридный поиск: BM25 + векторный + графовый**
- BM25 (ключевые слова) + cosine similarity (семантика) + knowledge graph (связи)
- Слитие через Reciprocal Rank Fusion (RRF, k=60)
- Реализация на Python: `rank_bm25` / `whoosh` + `sentence-transformers` + numpy
- Хранение: SQLite FTS5 для текста, in-memory numpy-массивы для векторов

**3. Не-деструктивная компрессия**
- Текущая реализация: `DELETE` старых сообщений после LLM-сжатия — данные теряются навсегда.
- Целевая: помечать `archived=True`, хранить полную историю для поиска,
  в LLM-контекст инжектить только сжатое саммари.

**4. Project profile (профиль проекта)**
- При первом обращении собрать ключевые концепции, используемые файлы, паттерны.
- Кэшировать в `repo_maps` (таблица уже существует в БД).
- Инжектить при каждом запросе к проекту — аналог `memory_profile` из agentmemory.

**5. Автодедуп наблюдений**
- SHA-256 хеш tool_output с 5-минутным окном — защита от дублей.
- Применимо к `tool_usage_stats` (таблица уже есть).

**6. Memory decay (затухание)**
- Кривая Эббингауза: часто используемые факты усиливаются, редкие — затухают.
- Авто-эвикция неактуальных данных (retention policy).
- Полезно для long-lived проектов с сотнями сессий.

### Почему прямая интеграция невозможна

1. **Разные стеки**: agentmemory = TypeScript (ESM) + iii-engine (Rust/WASM),
   fosved-coder = Python 3.12 (FastAPI). Полная несовместимость.
2. **Дополнительный Node.js runtime** в деплое — отдельный сервис, оверхед.
3. **Разные БД/ORM**: agentmemory использует iii-engine StateModule (SQLite),
   fosved-coder — SQLAlchemy (PostgreSQL/SQLite).
4. **Хуки не применимы**: agentmemory захватывает действия через 12 хуков
   для Claude Code/Cursor; fosved-coder использует собственный WS-агент-луп.
5. **21,800 LOC TypeScript** — нереально поддерживать как зависимость в Python-проекте.

### Предлагаемая архитектура (нативная Python-реализация)

```
core/
├── memory.py              # (текущий) CRUD + retrieval
├── memory_search.py       # (новый) BM25 + векторный поиск
├── memory_consolidate.py  # (новый) 4-уровневая консолидация
├── memory_profile.py      # (новый) профиль проекта
└── memory_decay.py        # (новый) retention policy + decay

db/
├── chat_history           # (текущий) + колонка archived: bool
├── chat_history_fts       # (новый) FTS5 виртуальная таблица
├── memory_facts           # (новый) semantic facts + embedding
├── memory_sessions        # (новый) эпизодические саммари сессий
└── memory_patterns        # (новый) процедурные паттерны
```

**Зависимости (Python, минимальные):**
- `sentence-transformers` + `numpy` — векторные эмбеддинги (локально, без API)
- `whoosh` или `rank_bm25` — полнотекстовый BM25-поиск
- Или чистый `sqlite3 FTS5` (уже встроен в Python 3.12) — нулевые зависимости

### Low-hanging fruit (можно реализовать до Слоя 3)

Независимо от полной реализации Phase 6, несколько идей внедрить сейчас:

| Идея | Сложность | Время | Влияние |
|------|-----------|-------|---------|
| FTS5 на `chat_history.content` | Низкая | 30 мин | Мгновенный поиск по истории |
| Не-деструктивная компрессия (`archived=True`) | Низкая | 1 час | Сохранение данных |
| Project profile в `repo_maps` | Низкая | 2 часа | Лучший контекст |
| SHA-256 дедуп tool outputs | Низкая | 1 час | Меньше шума |
| Memory decay (cron eviction) | Средняя | 3 часа | Автоочистка |

### Предусловия для полной реализации

- [ ] Фаза 1–4 V2 завершены
- [ ] `agent.py` разбит на модули (Слой 3)
- [ ] БД мигрирована на PostgreSQL (для `pgvector`, если нужен продакшн-масштаб)
- [ ] Проведён бенчмарк retrieval accuracy на реальных проектах fosved-coder
- [ ] Оценена нагрузка на БД при индексации векторных эмбеддингов

### Приоритет относительно CodeGraph (Phase 5)

CodeGraph **релевантнее** для fosved-coder на текущем этапе:
- CodeGraph понимает **структуру кода** (AST, call graph, framework-aware)
- Smart Memory понимает **историю чатов** (семантический поиск по фактам/сессиям)
- Для coding-ассистента знание кода > знание истории чатов

Порядок: Phase 5 (CodeGraph) → Phase 6 (Smart Memory).
